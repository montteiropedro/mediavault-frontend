import { Construction } from 'lucide-react';

export function SearchPage() {
  return (
    <div className="absolute inset-0 flex flex-col items-center justify-center text-zinc-100 gap-2">
      <Construction className="size-10" />
      <span>Página de busca em construção.</span>
    </div>
  );
}
